{
  "name": "spin-agent",
  "version": "0.1.0",
  "description": "Spawn a sub-agent with the correct claude-code-modes preset for a given stage. Assembles the agent's context (relevant planning docs, work unit spec, skills) before invocation. Used by the orchestrator to delegate stage work.",
  "capabilities": {
    "filesystem": "read",
    "network": false,
    "github_push": false,
    "shell_exec": false
  },
  "dependencies": [
    "workflow-utils/context-assembly"
  ]
}
