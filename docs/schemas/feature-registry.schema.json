{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "feature-registry",
  "title": "Feature Registry",
  "description": "L1 document: authoritative list of all features with metadata and dependency map",
  "type": "object",
  "required": ["doc_type", "version", "status", "created", "revised", "revision"],
  "additionalProperties": true,
  "properties": {
    "doc_type": {
      "type": "string",
      "const": "feature-registry"
    },
    "version": {
      "type": "integer",
      "minimum": 1
    },
    "status": {
      "type": "string",
      "enum": ["draft", "approved"]
    },
    "created": {
      "type": "string",
      "pattern": "^\\d{4}-\\d{2}-\\d{2}$"
    },
    "revised": {
      "type": "string",
      "pattern": "^\\d{4}-\\d{2}-\\d{2}$"
    },
    "revision": {
      "type": "integer",
      "minimum": 1
    }
  }
}
